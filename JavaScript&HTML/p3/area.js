var num = +prompt("Enter the radius of a circle:");
function circarea(radius)
{
	return area = radius*radius*Math.PI;
}

document.write("The area of a circle with radius "+num+" is "+circarea(num)+".");